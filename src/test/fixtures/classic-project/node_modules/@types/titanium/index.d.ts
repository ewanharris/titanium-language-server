// A stub standing in for @types/titanium. It mirrors the real package's shape — a Titanium
// namespace of classes with an alias — rather than its size: the real index.d.ts is 2.3MB and
// 66k lines, and committing it would say nothing the resolution tests do not.
//
// The version in package.json is deliberately 9.2.2 while classic-project's tiapp says 12.4.0.GA,
// so a test can tell "the project's own copy won" from "the version rule picked 12.x".

declare namespace Titanium {
	namespace UI {
		class View {
			backgroundColor: string;
			addEventListener (name: string, callback: () => void): void;
		}

		class Label extends View {
			/** The text to display */
			text: string;
			color: string;
		}

		function createLabel (options?: Partial<Label>): Label;
		function createView (options?: Partial<View>): View;
	}

	namespace App {
		const id: string;
	}
}

declare const Ti: typeof Titanium;
